version: '3'

services:
    # frontend:
    #     build: frontend
    #     ports:
    #         - 80:3000
    #     env_file:
    #         - .env
    api:
        build: api
        env_file:
            - example_env
            - fodler/example_env2
        ports:
            - 8090:80
        env_file:
            - .env
